// Find_Calculation.h

#ifndef FIND_CALCULATION_H
#define FIND_CALCULATION_H

#include <cmath>

namespace MathCalculations {
    // Function to calculate the square root of a double value
    double calculateSquareRoot(double value);

    // Function to calculate the cube of a double value
    double calculateCube(double value);

    // Function to calculate the power of a double value raised to a given exponent
    double calculatePower(double base, double exponent);
}

#endif // FIND_CALCULATION_H
